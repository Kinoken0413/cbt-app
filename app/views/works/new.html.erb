<div class="contents row">
  <div class="container">
    <h3>投稿する</h3>
    <%= form_with(model: @work, local: true) do |form| %>
      <%= form.text_area :automatic_thought, placeholder: "自動思考" %>
      <%#= form.text_field :image, placeholder: "Image Url" %>
      <%= form.text_area :text, placeholder: "合理的な反応", rows: "10" %>
      <%= form.submit "SEND" %>
    <% end %>
  </div>
</div>